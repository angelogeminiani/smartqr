/*!
 * SmartForge - test_settings
 * Copyright(c) 2011 Gian Angelo Geminiani
 * MIT Licensed
 */

var assert = require('assert')
    , application = require('../lib/application')
    , utils = require('../lib/utils.js')
    , utilsFs = require('../lib/utilsFs.js')
    ;


(function(){

    var path = utilsFs.pathResolve(__dirname, '..', '_workspace'),
        settingsroot = new application.Settings({path:path});

    assert.ok(null!=settingsroot.settings.http);
    console.log(utils.inspect(settingsroot.settings));

})();